# Optional: external ATS URL for “Go to ATS” on the job board when logged in as HR.
VITE_ATS_URL=

# Public careers site origin used in CV Analyser invite links.
# VITE_PUBLIC_APP_URL=https://career.indiraivf.in

# API base (empty in dev — Vite proxies /api to backend)
VITE_API_URL=

# Claude proxy path
VITE_CLAUDE_API_URL=/api/messages
